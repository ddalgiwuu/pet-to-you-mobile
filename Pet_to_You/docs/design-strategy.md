# Pet to You - 디자인 전략

> Simple Premium with Heart - 심플하지만 프리미엄하고, 따뜻한 감성을 담은 디자인

**버전**: v2.1
**작성일**: 2025-12-29
**디자인 철학**: Simple + Premium + Heart

---

## 📐 디자인 철학

### "Simple Premium with Heart"

**Simple** (심플함):
- 토스의 Simplicity 철학
- 불필요한 요소 제거
- 명확한 정보 계층

**Premium** (고급스러움):
- Glassmorphism (반투명 카드)
- 우아한 애니메이션
- 세련된 타이포그래피

**Heart** (따뜻함):
- 반려동물 일러스트
- 따뜻한 컬러 팔레트
- 감성적인 마이크로카피

---

## 🎨 2025 디자인 트렌드 적용

### 1. 미니멀리즘 2.0
**특징**: 화이트 스페이스 + 볼드 악센트 컬러

**적용**:
- 넉넉한 여백 (32px, 48px)
- 단순한 레이아웃
- 강렬한 CTA (노란색)
- 읽기 쉬운 타이포그래피

**예시**:
```
┌─────────────────────────────┐
│                             │  ← 48px top padding
│   [큰 제목]                 │
│                             │  ← 16px spacing
│   [설명 텍스트]             │
│                             │  ← 32px spacing
│   ┌───────────────────┐    │
│   │  [노란색 CTA 버튼] │    │
│   └───────────────────┘    │
│                             │  ← 48px bottom
└─────────────────────────────┘
```

---

### 2. Glassmorphism (유리 형태)
**특징**: 반투명 + 블러 + 부드러운 그림자

**적용 위치**:
- 카드 컴포넌트 (병원, 유치원 카드)
- 모달/바텀시트
- 네비게이션 바
- 통계 위젯

**CSS 구현**:
```css
.glass-card {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.18);
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.15);
  border-radius: 16px;
}

/* 어두운 배경 위 */
.glass-card-dark {
  background: rgba(0, 0, 0, 0.3);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}
```

**예시 (병원 카드)**:
```
┌───────────────────────────────┐
│ [흐릿한 배경 이미지]           │
│ ┌─────────────────────────┐  │
│ │ 반투명 카드 (Glass)      │  │ ← Glassmorphism
│ │                         │  │
│ │ 도그마루 메디컬 센터     │  │
│ │ ⭐ 4.9 (2,024)          │  │
│ │ 0.9km                   │  │
│ └─────────────────────────┘  │
└───────────────────────────────┘
```

---

### 3. Neumorphism (부드러운 UI)
**특징**: Soft Shadow + Highlight로 3D 효과

**적용 위치**:
- 버튼 (Primary CTA)
- 토글 스위치
- 슬라이더 (가격 필터)

**CSS 구현**:
```css
.neuro-button {
  background: #F8E318;
  border-radius: 26px;
  box-shadow:
    8px 8px 16px rgba(0, 0, 0, 0.1),
    -8px -8px 16px rgba(255, 255, 255, 0.7);
}

.neuro-button:active {
  box-shadow:
    inset 4px 4px 8px rgba(0, 0, 0, 0.1),
    inset -4px -4px 8px rgba(255, 255, 255, 0.7);
}
```

---

### 4. 마이크로 인터랙션
**목적**: 사용자 액션에 즉각적이고 즐거운 피드백

**구현 예시**:

**좋아요 버튼**:
```
클릭 → 하트 크기 증가 (scale 1.3) → 색상 변경 → 원래 크기
[애니메이션: 300ms ease-out]
```

**예약 완료**:
```
제출 → 로딩 스피너 → 체크마크 등장 (scale + fade) → confetti 🎉
[애니메이션: 총 1500ms]
```

**리스트 아이템 로드**:
```
각 카드가 순차적으로 fade-in
[stagger: 50ms씩 지연]
```

---

## 🎨 컬러 시스템 (확장)

### Primary Colors (기존)
```css
--primary-yellow: #F8E318;   /* 메인 브랜드 */
--primary-black: #1C1B1F;    /* 텍스트 */
--primary-white: #FFFFFF;    /* 배경 */
```

### Service Colors (서비스별 악센트)
```css
--hospital-blue: #4A90E2;    /* 병원 - 신뢰, 전문성 */
--daycare-orange: #FF9F43;   /* 유치원 - 활기, 따뜻함 */
--adoption-pink: #FFB6C1;    /* 입양 - 사랑, 감성 */
--insurance-green: #52C41A;  /* 보험 - 안정, 보장 */
```

### Gradients (프리미엄 느낌)
```css
--gradient-primary: linear-gradient(135deg, #F8E318 0%, #FFB547 100%);
--gradient-hospital: linear-gradient(135deg, #4A90E2 0%, #357ABD 100%);
--gradient-daycare: linear-gradient(135deg, #FF9F43 0%, #FF6B35 100%);
--gradient-adoption: linear-gradient(135deg, #FFB6C1 0%, #FF69B4 100%);
--gradient-insurance: linear-gradient(135deg, #52C41A 0%, #389E0D 100%);
```

### Glassmorphism
```css
--glass-white: rgba(255, 255, 255, 0.7);
--glass-dark: rgba(0, 0, 0, 0.3);
--glass-border: rgba(255, 255, 255, 0.18);
```

---

## 📱 컴포넌트 디자인

### 병원 카드 (Glassmorphism)

**디자인 특징**:
- 반투명 배경
- 블러 효과
- 부드러운 그림자
- 호버 시 약간 들어 올림

**Tailwind CSS**:
```tsx
<div className="
  bg-white/70
  backdrop-blur-md
  border border-white/20
  rounded-2xl
  p-4
  shadow-[0_8px_32px_0_rgba(31,38,135,0.15)]
  hover:shadow-[0_12px_40px_0_rgba(31,38,135,0.25)]
  hover:-translate-y-1
  transition-all duration-300
">
  {/* 병원 정보 */}
</div>
```

---

### Primary Button (Neumorphism)

**디자인 특징**:
- Soft Shadow로 3D 효과
- 눌렀을 때 inset shadow
- 부드러운 터치 피드백

**Tailwind CSS** (커스텀 유틸리티 필요):
```tsx
<button className="
  bg-[#F8E318]
  text-black
  font-medium
  px-8 py-4
  rounded-[26px]
  shadow-[8px_8px_16px_rgba(0,0,0,0.1),-8px_-8px_16px_rgba(255,255,255,0.7)]
  active:shadow-[inset_4px_4px_8px_rgba(0,0,0,0.1),inset_-4px_-4px_8px_rgba(255,255,255,0.7)]
  transition-all duration-200
  hover:scale-105
">
  예약하기
</button>
```

---

### 하단 탭바 (플로팅 + Glass)

**디자인 특징**:
- 플로팅 (화면 하단에서 떠있음)
- Glassmorphism 효과
- 선택된 탭만 아이콘 + 라벨

**구조**:
```
┌─────────────────────────────────┐
│                                 │
│  [콘텐츠 영역]                   │
│                                 │
│                                 │
├─────────────────────────────────┤
│         ┌─────────┐             │  ← 16px bottom margin
│    [홈] │[병원]  │ [입양] [마이]│  ← Glass 플로팅 탭
│         └─────────┘             │
└─────────────────────────────────┘
```

---

## 🖼️ 일러스트 스타일

### 반려동물 일러스트

**스타일**: Flat Illustration + Rounded Shapes
- 귀여운 강아지/고양이
- 파스텔 컬러
- 둥근 형태
- 표정 있는 캐릭터

**활용 위치**:
- 빈 상태 (Empty State)
- 로딩 화면
- 온보딩
- 404 페이지
- 성공 화면 (입양 완료 등)

**예시**:
```
입양 완료 화면:
  [귀여운 강아지 일러스트 + 하트]
  "새로운 가족을 만나셨어요!"
  [따뜻한 메시지]
```

---

## 🎬 애니메이션 가이드

### Timing (Easing Functions)
```css
--ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);     /* 진입 */
--ease-in-expo: cubic-bezier(0.7, 0, 0.84, 0);     /* 퇴장 */
--ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55); /* 바운스 */
```

### Duration
```css
--duration-fast: 150ms;      /* 버튼 피드백 */
--duration-normal: 300ms;    /* 화면 전환 */
--duration-slow: 500ms;      /* 복잡한 애니메이션 */
```

### 주요 애니메이션

**1. 카드 호버**:
```css
.card {
  transition: all 300ms cubic-bezier(0.16, 1, 0.3, 1);
}
.card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 12px 40px rgba(31, 38, 135, 0.25);
}
```

**2. 버튼 클릭**:
```css
.button:active {
  transform: scale(0.95);
  transition: transform 100ms;
}
```

**3. 페이지 전환**:
```tsx
// Framer Motion
<motion.div
  initial={{ opacity: 0, x: 20 }}
  animate={{ opacity: 1, x: 0 }}
  exit={{ opacity: 0, x: -20 }}
  transition={{ duration: 0.3, ease: [0.16, 1, 0.3, 1] }}
>
  {children}
</motion.div>
```

---

## 📐 반응형 디자인

### Breakpoints
```css
--screen-sm: 640px;   /* 모바일 */
--screen-md: 768px;   /* 태블릿 */
--screen-lg: 1024px;  /* 데스크톱 */
--screen-xl: 1280px;  /* 큰 데스크톱 */
```

### 레이아웃 전략
- **모바일 우선** (Mobile First)
- **단일 컬럼** (< 768px)
- **2-3 컬럼** (768px-1024px)
- **사이드바 + 메인** (> 1024px)

---

## 🌗 다크 모드 (Phase 2)

### 컬러 조정
```css
/* Light Mode */
--bg-primary: #FFFFFF;
--text-primary: #1C1B1F;

/* Dark Mode */
--bg-primary: #1C1B1F;
--text-primary: #F9FAFB;
--glass-dark: rgba(255, 255, 255, 0.1);
```

---

## 🎯 서비스별 디자인 컨셉

### 🏥 병원 (신뢰 & 전문성)
- **컬러**: Blue (#4A90E2)
- **스타일**: 깔끔, 전문적
- **아이콘**: Medical, Clean
- **일러스트**: 신뢰감 있는 수의사

### 🏫 유치원 (활기 & 안전)
- **컬러**: Orange (#FF9F43)
- **스타일**: 활발, 따뜻함
- **아이콘**: Play, Happy
- **일러스트**: 신나게 노는 강아지들

### 🐕 입양 (사랑 & 감성)
- **컬러**: Pink (#FFB6C1)
- **스타일**: 부드럽고 감성적
- **아이콘**: Heart, Home
- **일러스트**: 행복한 입양 장면

### 💰 보험 (안정 & 보장)
- **컬러**: Green (#52C41A)
- **스타일**: 안정적, 신뢰
- **아이콘**: Shield, Check
- **일러스트**: 보호받는 반려동물

---

## 참고 자료

**2025 트렌드**:
- [모바일 앱 UI/UX 트렌드 2025](https://www.chopdawg.com/ui-ux-design-trends-in-mobile-apps-for-2025/)
- [16 Key Mobile App Trends](https://spdload.com/blog/mobile-app-ui-ux-design-trends/)
- [2025-2026 디자인 트렌드](https://www.mindinventory.com/blog/mobile-app-ui-ux-design-trends/)

**Glassmorphism**:
- [Glassmorphism 가이드](https://www.weavely.ai/blog/the-glassmorphism-design-trend-in-figma)
- [10 Glassmorphism 예시](https://onyx8agency.com/blog/glassmorphism-inspiring-examples/)

**한국 UI/UX**:
- [Korean UI/UX 차별점](https://teqnoid.com/the-logic-behind-korean-ui-ux-design/)
- [토스 Simplicity](https://www.ideausher.com/blog/what-is-super-app-toss/)

---

*다음 단계: Magic MCP로 실제 컴포넌트 생성*
